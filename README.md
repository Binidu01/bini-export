# bini-export
Pure static SPA export for bini-router projects. Pre-renders every route, generates the right 404.html, and strips all platform server files — leaving dist/ ready for GitHub Pages, Netlify static, S3, and any other static host.
